import { Card } from "@mui/joy";
import React from "react";

interface SentenceProps {
  sentence: string;
}

const Sentence = ({ sentence }: SentenceProps) => {
  return <Card>Sentence</Card>;
};

export default Sentence;
